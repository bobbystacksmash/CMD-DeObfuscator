const assert = require("chai").assert;

describe("DeObfuscator Tests", () => {

    beforeEach(function () {});

    xdescribe("XX", () => {
        it("should xx", () => {
            assert.equal(1, 2);
        });
    });
});
